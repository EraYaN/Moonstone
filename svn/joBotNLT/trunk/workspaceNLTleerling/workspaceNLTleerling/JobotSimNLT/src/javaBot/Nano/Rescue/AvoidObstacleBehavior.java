package javaBot.Nano.Rescue;

import com.muvium.apt.PeriodicTimer;

/**
 * AvoidObstacleBehavior
 * Contains a numbre of functions to be written hy the student that will:
 * 
 * - Find an object
 * - Estimate the size of an object
 * - Dribble with a ball
 * - Avoid abstacles on your path
 */

public class AvoidObstacleBehavior extends Behavior{
	
	public AvoidObstacleBehavior(BaseController initJoBot, PeriodicTimer initServiceTick,
			int servicePeriod) 
	{
		super(initJoBot, initServiceTick, servicePeriod);
	}

	public void doBehavior()
	{
		int s2 = 0;
	}

// - Find an object
// - Estimate the size of an object
// - Dribble with a ball
// - Avoid abstacles on your path

}
